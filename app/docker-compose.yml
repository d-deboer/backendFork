version: "3.8"

services:
  nest:
    container_name: nest
    build: .
    image: nestapp:nest
    tty: true
    stdin_open: true
  # change the $(pwd) with the result of the "pwd" command executed inside the hackathon directory
    volumes:
      - $(pwd):/app
